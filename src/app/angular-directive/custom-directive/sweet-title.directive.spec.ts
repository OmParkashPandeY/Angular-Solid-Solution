import { SweetTitleDirective } from './sweet-title.directive';

describe('SweetTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new SweetTitleDirective();
    expect(directive).toBeTruthy();
  });
});
