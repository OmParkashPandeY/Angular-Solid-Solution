import { SquireRootPipe } from './squire-root.pipe';

describe('SquireRootPipe', () => {
  it('create an instance', () => {
    const pipe = new SquireRootPipe();
    expect(pipe).toBeTruthy();
  });
});
